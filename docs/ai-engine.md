# AI engine

FastAPI uses no remote model or GPU. RCA is a weighted graph scorer, prediction is a latency/error heuristic, and counterfactual results are explicitly `SIMULATED` and `MODEL-ESTIMATED`.
